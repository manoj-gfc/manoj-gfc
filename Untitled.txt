

Please refactor my project by separating HTML, CSS, and JavaScript properly.

Requirements:

1. Remove all inline CSS and <style> blocks from the HTML files and move them into the appropriate external CSS files.

2. Remove all inline JavaScript, <script> blocks, and inline event handlers (such as onclick, onchange, oninput, etc.) from the HTML files and move the logic into the appropriate external JavaScript files.

3. Keep only the required HTML markup.

4. Link the existing external CSS and JavaScript files correctly in the HTML.

5. Do not create unnecessary CSS or JavaScript files. Reuse the existing files whenever possible.

6. Do not change the website's design, layout, responsiveness, animations, functionality, SEO, accessibility, or user experience.

7. Keep the HTML clean, semantic, valid, and production-ready.

8. Return the updated HTML files. If any CSS or JavaScript code is moved, place it in the appropriate existing CSS or JavaScript file.

Keep the HTML file as clean as possible. The HTML should contain only the structure and content. All styling must be in CSS files, and all behavior must be in JavaScript files.




Please review my HTML file and make only the following improvements.

Requirements:

1. Remove target="_blank" from all internal links (such as contact.html). Keep target="_blank" only for external websites.

2. Replace the current pinterest share URL with my official pinterest URL:
  https://www.pinterest.com/gfcbuilder


3. If the copyright year is hardcoded (for example, Copyright © 2026), replace it with:
   <span id="year"></span>
   and automatically display the current year using JavaScript.

4. Do not make any other changes.

5. Do not modify the HTML structure, design, layout, responsiveness, animations, CSS styling, JavaScript functionality, SEO, accessibility, or user experience.

6. Keep the code clean, valid, well-organized, and production-ready.

7. Before making any change, verify that it will not affect the appearance or functionality of the website.

Please return the updated HTML and JavaScript files with only these requested changes.
If the footer in index.html is already the best version, treat it as the single source of truth and make every other page match it exactly.You added new inline JavaScript inside the HTML file. This is not what I requested.

Please remove all newly added inline JavaScript from the HTML.

Do not add, modify, or duplicate any JavaScript code inside the HTML.

Each page must continue using its existing external JavaScript file only (for example, js/script.js).

Do not load the same JavaScript file more than once.

Modify only the HTML where necessary. Do not change any JavaScript files.Please use the footer from my index.html file as the standard footer for all other HTML pages.

Requirements:

1. Copy the footer from index.html exactly as it is and replace the existing footer in all other HTML pages.

2. Keep the same HTML structure, content, links, icons, contact information, social media links, and CSS class names as the footer in index.html.

3. Modify only the HTML footer section. Do not edit any CSS or JavaScript files.

4. Each page should continue using its own existing CSS and JavaScript files. Do not add, remove, merge, or modify any CSS or JS files.

5. If the footer HTML contains any validation errors (such as unclosed tags or invalid nesting), fix only those errors while keeping the same appearance and functionality.

6. Ensure the footer is identical across all pages for consistency.

7. Do not modify any page-specific content outside the footer.

8. Do not create new CSS classes or duplicate footer styles. Reuse the existing CSS already linked to each page.

9. Do not change the design, layout, responsiveness, animations, SEO, accessibility, or JavaScript functionality.

10. If the footer in index.html is already the best version, treat it as the single source of truth and make every other page match it exactly.

11. Return only the updated HTML files. Do not return or modify any CSS or JavaScript files unless an HTML change absolutely requires an existing CSS or JS reference.